{
  "name": "@tin4/tanstack-start",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp src/index.ts dist/index.ts",
    "dev": "bun --watch src/index.ts",
    "lint": "echo 'No lint configured for @tin4/tanstack-start yet'",
    "typecheck": "echo 'No typecheck configured for @tin4/tanstack-start yet'"
  }
}
