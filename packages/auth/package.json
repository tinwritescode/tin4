{
  "name": "@tin4/auth",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp src/index.ts dist/index.ts",
    "lint": "echo 'No lint configured for @tin4/auth yet'",
    "typecheck": "echo 'No typecheck configured for @tin4/auth yet'"
  }
}
